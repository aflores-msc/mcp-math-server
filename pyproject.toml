[project]
name = "mcp-math-project"
version = "0.1.0"
description = "A simple MCP server and client for adding numbers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=0.1.0",
    "mcp>=0.1.0",
    "httpx>=0.27.0"
]
